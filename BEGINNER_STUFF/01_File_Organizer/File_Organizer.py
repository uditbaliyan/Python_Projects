import os

directory_path = '.'  # Current directory or specify another path

# List all files with their full path
all_files = os.listdir(directory_path)
print("All files in the directory:", all_files)

def structure_files_my_name(path:str)->None:
    """
    Purpose: 
    """
    
# end def
